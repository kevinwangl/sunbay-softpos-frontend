# 内核管理功能快速开始

## 访问内核管理页面

1. 登录系统后，在左侧菜单中找到"内核管理"（图标：</>）
2. 点击进入内核管理页面

## 上传内核

### 步骤：

1. 点击页面右上角的"上传内核"按钮
2. 在弹出的对话框中：
   - **版本号**：输入版本号，格式必须为 `vX.Y.Z`（例如：`v1.0.0`）
   - **内核文件**：拖拽或点击上传 `.wasm` 文件
3. 点击"上传"按钮
4. 等待上传完成，系统会显示成功消息

### 注意事项：

- 版本号必须以 `v` 开头，后跟三个数字，用点分隔（如：v1.0.0）
- 只能上传 `.wasm` 格式的文件
- 文件大小不能超过 50MB
- 版本号不能重复

## 查看内核列表

### 列表信息：

- **版本**：内核版本号
- **状态**：
  - 🟢 **稳定版**：已发布，可供设备使用
  - ⚪ **草稿**：已上传但未发布
  - 🟡 **已弃用**：不再推荐使用
- **文件大小**：内核文件大小
- **文件哈希**：文件的SHA256哈希值（用于验证完整性）
- **创建时间**：上传时间
- **更新时间**：最后修改时间

### 筛选功能：

使用页面右上角的"筛选状态"下拉框，可以按状态筛选内核：
- 草稿
- 稳定版
- 已弃用

## 发布内核

### 步骤：

1. 找到状态为"草稿"的内核
2. 点击该行的"发布"按钮（绿色对勾图标）
3. 在确认对话框中点击"确定"
4. 内核状态将变更为"稳定版"

### 说明：

- 只有草稿状态的内核可以发布
- 发布后的内核可以被设备下载和使用
- 发布是不可逆的操作

## 下载内核

### 步骤：

1. 找到要下载的内核
2. 点击该行的"下载"按钮（下载图标）
3. 浏览器会自动下载文件，文件名格式为 `kernel-{version}.wasm`

### 说明：

- 所有状态的内核都可以下载
- 下载的文件可用于备份或分发

## 删除内核

### 步骤：

1. 找到要删除的内核（必须是非稳定版）
2. 点击该行的"删除"按钮（红色删除图标）
3. 在确认对话框中点击"确定"
4. 内核将被永久删除

### 注意事项：

- ⚠️ **稳定版内核不能删除**（保护生产环境）
- 删除操作不可恢复
- 删除后，相关文件也会从服务器上移除

## 常见问题

### Q: 为什么我不能删除某个内核？
A: 稳定版内核不能删除，这是为了保护生产环境。如果确实需要移除，可以先将其标记为"已弃用"。

### Q: 上传失败怎么办？
A: 请检查：
1. 文件格式是否为 `.wasm`
2. 文件大小是否超过 50MB
3. 版本号格式是否正确（vX.Y.Z）
4. 版本号是否已存在

### Q: 如何知道哪个版本正在被使用？
A: 当前版本仅显示内核列表，后续版本会添加使用统计功能，显示每个内核版本被多少设备使用。

### Q: 可以修改已上传的内核吗？
A: 不可以直接修改。如需更新，请上传新版本。

### Q: 版本号有什么规范？
A: 遵循语义化版本规范：
- 主版本号（Major）：不兼容的API修改
- 次版本号（Minor）：向下兼容的功能性新增
- 修订号（Patch）：向下兼容的问题修正

例如：
- v1.0.0 → v1.0.1：修复bug
- v1.0.0 → v1.1.0：新增功能
- v1.0.0 → v2.0.0：重大更新

## 最佳实践

1. **版本管理**：
   - 使用语义化版本号
   - 保持版本号连续性
   - 为每个版本做好记录

2. **测试流程**：
   - 上传后先保持草稿状态
   - 在测试环境充分测试
   - 确认无误后再发布为稳定版

3. **备份策略**：
   - 定期下载重要版本作为备份
   - 保留历史版本以便回滚

4. **安全考虑**：
   - 验证文件哈希值确保完整性
   - 只从可信来源上传内核
   - 定期审查已上传的内核

## 技术支持

如遇到问题，请联系技术支持团队或查看完整文档：
- 功能文档：`KERNEL_MANAGEMENT_FEATURE.md`
- API文档：`sunbay-softpos-backend/API_DOCUMENTATION.md`
